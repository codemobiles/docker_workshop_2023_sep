{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/demo1_dockerfile/package.json",
        "localRoot": "${workspaceFolder}/demo1_dockerfile"
      }
    }
  ]
}
